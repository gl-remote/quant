# ============================================================
# 本地覆盖配置 - 不提交到git
# 此文件包含敏感信息和本地环境配置
#
# 设计说明 (Design Note):
#   API 凭证推荐使用环境变量（优先级最高，不会被写入任何文件）:
#     export TQSDK_API_KEY="your_key"
#     export TQSDK_API_SECRET="your_secret"
#   也可以在此文件中填写凭证（备选方案），但环境变量具有更高优先级。
#   该文件已在 .gitignore 中排除，不会提交到版本控制。
# ============================================================

# 第三方服务配置
[[third_party.services]]
name = "tqsdk"                  # 天勤量化
provider = "tianqin"
api_key = "gaolei"   # 或设置环境变量 TQSDK_API_KEY
api_secret = "iVxevgr4p!hQXd!"  # 或设置环境变量 TQSDK_API_SECRET
enabled = true

# 本地开发环境覆盖
[environment]
name = "local"
debug = true

# 未来可扩展的配置
# strategy_params:
#   custom_params: []

# 第三方服务密钥
# [[third_party.services]]
# name = "binance"
# api_key = "xxx"
# api_secret = "xxx"
